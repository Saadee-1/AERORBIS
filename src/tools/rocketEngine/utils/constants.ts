/**
 * Physical constants for rocket engine calculations
 */

export const G0 = 9.80665; // Standard gravity (m/s²)
export const R_UNIVERSAL = 8314.4621; // Universal gas constant (J/(kmol·K))

// Default values
export const DEFAULT_GAMMA = 1.22; // Typical for LOX/RP-1
export const DEFAULT_NOZZLE_EFFICIENCY = 0.98;
export const DEFAULT_CSTAR_EFFICIENCY = 0.95;
export const DEFAULT_PRESSURE_LOSS = 0.02; // 2% pressure loss
