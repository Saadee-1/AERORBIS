"use client";

// Audio visualizer disabled - using GalaxyBackground instead
// This prevents the createMediaElementSource re-connection error

const AudioVisualizer = () => {
  return null;
};

export default AudioVisualizer;
